CREATE TABLE T_CAR(
                      id INT AUTO_INCREMENT  PRIMARY KEY,
    NAME VARCHAR(255) not null
);

CREATE TABLE T_TRUCK(
                        id INT AUTO_INCREMENT  PRIMARY KEY,
    NAME VARCHAR(255) not null
);
